<h1>{{title}}</h1>
<div>
    {{#each phones}}
        <div class="phoneBox center">
            <div><a href="product/{{this.id}}">{{this.name}}</a></div>
            <div>{{this.short_description}}</div>
            <div>{{this.price}}лв.</div>
            <div>
                <img src="{{this.image_url}}" alt="Phone Image">
            </div>
        </div>
    {{/each}}
    <br/>
    <br/>
    <a href="/admin">Администрация</a>
</div>
